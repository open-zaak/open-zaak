---

# Runtime secrets. Values are base64 encoded

kind: Secret
apiVersion: v1
type: Opaque
data:
  SECRET_KEY: {{ secret_key | b64encode }}
  DB_PASSWORD: {{ db_password | b64encode }}
{% if sentry_dsn %}  SENTRY_DSN: {{ sentry_dsn | b64encode }}{% endif %}
