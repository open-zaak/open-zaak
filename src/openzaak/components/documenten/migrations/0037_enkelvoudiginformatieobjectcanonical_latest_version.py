# SPDX-License-Identifier: EUPL-1.2
# Copyright (C) 2025 Dimpact
# Generated by Django 5.2.8 on 2025-12-18 13:29

import django.db.models.deletion
from django.db import migrations, models, connection


class Migration(migrations.Migration):
    dependencies = [
        ("documenten", "0036_reserveddocument"),
    ]

    operations = [
        migrations.AddField(
            model_name="enkelvoudiginformatieobjectcanonical",
            name="latest_version",
            field=models.OneToOneField(
                editable=False,
                null=True,
                on_delete=django.db.models.deletion.SET_NULL,
                related_name="latest_version_of",
                to="documenten.enkelvoudiginformatieobject",
            ),
        ),
        migrations.RunSQL(
            """
        UPDATE documenten_enkelvoudiginformatieobjectcanonical t
        SET latest_version_id = latest.latest_version_id
        FROM (
            SELECT DISTINCT ON(canonical_id)
            canonical_id, eio.id AS latest_version_id
            FROM documenten_enkelvoudiginformatieobject AS eio
            ORDER BY canonical_id, eio.versie DESC
        ) AS latest
        WHERE t.id = latest.canonical_id
        """,
            migrations.RunSQL.noop,
        ),
        migrations.RunSQL(
            """
            CREATE OR REPLACE FUNCTION set_latest_version() RETURNS TRIGGER AS $$
                DECLARE
                    selected_canonical_id INT;
                BEGIN
                    IF TG_OP = 'DELETE' THEN
                        selected_canonical_id := OLD.canonical_id;
                    ELSE
                        selected_canonical_id := NEW.canonical_id;
                    END IF;
                    UPDATE documenten_enkelvoudiginformatieobjectcanonical
                    SET latest_version_id = (
                        SELECT id
                        FROM documenten_enkelvoudiginformatieobject
                        WHERE canonical_id = selected_canonical_id
                        ORDER BY versie DESC
                        LIMIT 1
                    )
                    WHERE id = selected_canonical_id;
                    IF TG_OP = 'DELETE' THEN
                        RETURN null;
                    ELSE
                        RETURN NEW;
                    END IF;
                END;
            $$ LANGUAGE plpgsql;
            CREATE TRIGGER after_delete_update_canonical
                AFTER INSERT OR UPDATE OR DELETE ON documenten_enkelvoudiginformatieobject
                FOR EACH ROW EXECUTE FUNCTION set_latest_version();
            """,
            """
            DROP TRIGGER after_delete_update_canonical ON documenten_enkelvoudiginformatieobject;
            DROP FUNCTION IF EXISTS set_latest_version();
            """,
        ),
    ]
